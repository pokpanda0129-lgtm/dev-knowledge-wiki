# AGENTS.md

이 저장소는 개발 지식 위키를 만들고 관리하기 위한 프로젝트입니다.

Codex는 이 파일을 프로젝트의 기본 작업 지침으로 사용합니다. 사용자가 자연어로 문서 생성을 요청하면, 프로젝트 전용 skill과 `.codex` 아래의 에이전트, 위키 작업 가이드를 참고해 일관된 형식의 위키 문서를 만듭니다.

## 기본 원칙

- 사용자가 별도로 요청하지 않으면 모든 설명 문서는 한국어로 작성합니다.
- 파일명, 폴더명, 코드 식별자는 영어와 kebab-case를 사용합니다.
- 문서는 짧고 실무적으로 작성합니다.
- 비개발자도 문서 앞부분은 이해할 수 있게 씁니다.
- 기본 문서는 글만으로 장면이 떠오르게 씁니다.
- 이미지는 사용자가 요청했거나 글만으로 이해가 어려울 때만 추가합니다.

## 작업 태도

이 프로젝트에서는 속도보다 불필요한 변경을 줄이고, 사용자의 의도에 맞는 결과를 만드는 것을 우선합니다.

### 구현 전에 생각하기

- 확실하지 않은 내용은 추측하지 않습니다.
- 모호한 요구사항은 조용히 한 방향으로 정하지 않고, 가능한 해석을 드러냅니다.
- 전제가 있다면 먼저 짧게 밝힙니다.
- 더 단순한 접근이 있으면 말합니다.
- 요구가 불분명해서 결과물이 크게 달라질 수 있으면 먼저 질문합니다.

### 단순하게 만들기

- 요청받은 문제를 해결하는 최소한의 변경을 우선합니다.
- 요구되지 않은 기능을 추가하지 않습니다.
- 한 번만 쓰는 코드를 위해 추상화를 만들지 않습니다.
- 필요 없는 설정화나 확장성을 넣지 않습니다.
- 불가능한 상황까지 과하게 방어하는 코드를 만들지 않습니다.
- 구현이 길어졌다면 더 짧고 명확한 방법이 없는지 다시 확인합니다.

### 필요한 부분만 수정하기

- 기존 코드를 수정할 때는 요청과 직접 관련된 부분만 건드립니다.
- 주변 코드, 주석, 포맷을 이유 없이 개선하지 않습니다.
- 기존 스타일이 마음에 들지 않아도 프로젝트 스타일을 따릅니다.
- 관련 없는 죽은 코드나 이상한 구조를 발견하면 삭제하지 말고 언급만 합니다.
- 내가 만든 변경으로 생긴 미사용 import, 변수, 함수는 정리합니다.
- 변경한 모든 줄은 사용자의 요청과 연결되어 있어야 합니다.

### 목표와 검증 기준 갖기

- 작업을 시작할 때 성공 기준을 생각합니다.
- 버그 수정은 가능하면 재현 방법이나 테스트 기준을 먼저 잡습니다.
- 리팩터링은 동작이 유지되는지 확인할 수 있어야 합니다.
- 여러 단계 작업은 짧은 계획과 각 단계의 확인 방법을 둡니다.
- 검증할 수 없는 “잘 되게 해줘” 식 요청은 필요한 경우 기준을 물어봅니다.

## 자연어 요청 처리

사용자는 별도 커맨드를 외울 필요가 없습니다.

사용자가 `$dev-knowledge-wiki`, `skill`, `subagent`를 직접 언급하지 않아도, 개발 지식 위키 문서 생성/개선/리뷰/이미지 추가/README 정리 요청이면 `dev-knowledge-wiki` skill을 사용합니다.

작업이 여러 관점의 검토나 병렬 처리가 필요한 수준이면 사용자가 매번 "subagent를 써서"라고 말하지 않아도 실제 subagent 사용을 고려합니다. 단순 오타 수정, 링크 수정, 작은 문장 수정에는 subagent를 쓰지 않습니다.

예를 들어 다음 요청은 모두 위키 작업으로 처리합니다.

```text
frontend의 SSR과 CSR의 차이를 md로 만들어줘
java Stream map과 flatMap 차이를 정리해줘
frontend README에 문서 목록 정리해줘
이 글에 비개발자도 이해하기 쉬운 그림 추가해줘
```

자연어 요청 처리 흐름은 `.codex/wiki-guide.md`를 따릅니다.

## 문서 위치 규칙

도메인별 문서는 각 도메인 폴더 아래에 둡니다.

```text
frontend/
java/
```

카테고리가 분명하면 하위 폴더를 만듭니다.

```text
frontend/rendering/ssr-vs-csr.md
java/stream/map-vs-flatmap.md
```

이미지를 별도로 만들 때는 문서와 같은 카테고리 폴더의 `assets` 아래에 둡니다.

```text
frontend/rendering/assets/ssr-vs-csr-story.svg
```

## 기본 문서 구조

새 문서를 만들 때는 먼저 `skills/dev-knowledge-wiki/assets/article.md`를 기준으로 사용합니다.

주제가 특별히 다른 구성을 요구하지 않으면 아래 순서를 유지합니다.

```md
# 주제명

## 단어 뜻

## 예시 스토리

## 한 줄 요약

## 개념 설명
```

## `.codex` 구성

설치형 `skill`과 실제 `subagent` 실행은 역할이 다릅니다.

- `skill`: Codex가 자동 인식할 수 있는 설치형 작업 지식입니다. 이 프로젝트의 원본 skill은 `skills/dev-knowledge-wiki`에 둡니다.
- `subagent`: 사용자가 subagent, 병렬 작업, 위임 작업을 명시적으로 요청했을 때 실제 Codex 실행 기능으로 사용합니다.

### Subagent 운영

- 이 프로젝트는 Claude Code식 커스텀 subagent 파일을 흉내 내지 않습니다.
- 개발 위키 작업에서 계획, 예시 스토리 구성, 리뷰를 분리하면 품질이 좋아지는 경우 Codex의 실제 subagent 실행 기능을 사용합니다.
- 단순 작업에는 subagent를 쓰지 않고 메인 Codex가 바로 처리합니다.
- subagent 운영 지침은 `skills/dev-knowledge-wiki/references/subagents.md`를 따릅니다.

### 스킬

- `skills/dev-knowledge-wiki/SKILL.md`: 개발 지식 위키 문서를 생성하거나 개선할 때 사용하는 프로젝트 전용 설치형 skill입니다.
- `skills/dev-knowledge-wiki/references/writing.md`: 위키 문서 작성 규칙입니다.
- `skills/dev-knowledge-wiki/references/visuals.md`: 사용자가 이미지를 요청했을 때의 작성 규칙입니다.
- `skills/dev-knowledge-wiki/references/accuracy-review.md`: 비유, 단정 표현, 기술 사실 정확성 검증 규칙입니다.
- `skills/dev-knowledge-wiki/references/subagents.md`: 실제 Codex subagent 사용 조건과 역할 매핑입니다.

이 저장소의 skill 원본은 프로젝트 안에 둡니다. Codex가 자동 인식해야 한다면 `skills/dev-knowledge-wiki`를 Codex의 skill 설치 위치에 복사하거나 링크합니다.

### 위키 작업 가이드

- `.codex/wiki-guide.md`: 자연어 요청을 위키 작업으로 해석하는 내부 가이드입니다.

### 템플릿

- `skills/dev-knowledge-wiki/assets/article.md`: 새 위키 문서를 만들 때 사용하는 기본 문서 템플릿입니다.

## README 갱신

새 문서를 만들었고 도메인 README에 문서 목록이 있으면, 관련 링크를 추가합니다.

기존 README가 깨진 인코딩처럼 보이면 내용을 덮어쓰기 전에 원문 의도를 확인합니다.

## 품질 기준

문서를 완료하기 전에 다음을 확인합니다.

- 제목과 저장 경로가 주제와 맞는가
- 한 줄 요약이 있는가
- 단어 뜻과 예시 스토리가 초반에 분리되어 잡히는가
- 쉬운 비유가 기술적 사실을 왜곡하지 않는가
- 개념 설명이 짧고 실제 동작 중심인가
- 글만 읽어도 장면이 머릿속에 떠오르는가
- 문서가 너무 길지 않은가
